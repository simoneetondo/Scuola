spring.application.name=Scuola

# DEVELOPMENT: default to H2 in-memory so the app can run without external DB.
# If you want to use MySQL, comment the H2 properties below and uncomment MySQL block.

# H2 (in-memory)
# spring.datasource.url=jdbc:h2:mem:scuola;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update

# MySQL example (uncomment to use MySQL instead of H2)
# spring.datasource.url=jdbc:mysql://localhost:3306/scuola?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=changeme
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/project_school?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Lecce2024!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE